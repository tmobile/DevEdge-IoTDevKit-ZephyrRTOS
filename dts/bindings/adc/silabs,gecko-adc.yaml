description: Silabs Gecko ADC node

compatible: "silabs,gecko-adc"

include: adc-controller.yaml

properties:

  # "#io-channel-cells":
  #   const: 1

  "num-channels":
    type: int
    required: true
    description:
      Number of channels available

  frequency:
    type: int
    required: true
    description:
      This property selects the ADC source frequency.

io-channel-cells:
  - input
