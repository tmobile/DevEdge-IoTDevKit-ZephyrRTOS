tests:
  arch.arm.ramfunc:
    platform_exclude: efm32pg_stk3402a
    filter: CONFIG_ARCH_HAS_RAMFUNC_SUPPORT
    tags: arm userspace
    arch_allow: arm
    extra_configs:
      - CONFIG_PICOLIBC_HEAP_SIZE=0
